import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'home-task-control',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
